function selecionarImagem() {
  alert("Selecionar imagem ainda não implementado.");
}

document.getElementById("form-agendamento").addEventListener("submit", function (e) {
  e.preventDefault();
  alert("Agendamento confirmado (simulação). Aqui você pode integrar com o backend.");
});